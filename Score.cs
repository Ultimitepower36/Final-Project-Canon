class Score{

    public int score(int start, int change){
        // Updates the score with the following
        var currentscore = start;
        currentscore += change;

        return currentscore;
    }
}