class Player{
    
    public string player(string input){
        // variable decloration
        var ReturnVal = "";

        // sets the player to look like a #
        if (input == "PlayerID") {
            ReturnVal = "#";
        }
        return ReturnVal;
    }
}